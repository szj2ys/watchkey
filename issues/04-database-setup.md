# Issue 04: Database Schema and Setup

## Description
Setup Supabase database with required tables and relationships.

## Tasks
x Create `videos` table (id, youtube_id, title, channel, duration, thumbnail_url)
x Create `analyses` table (id, video_id, chapters, summary, transcript, status)
x Setup Row Level Security policies
x Create database indexes for performance
x Add database types to project

## Acceptance Criteria
- Tables created in Supabase
- TypeScript types generated
- Basic CRUD operations work
